#include "main.h"
#include <stdio.h>

/**
 * main - computes the absolute value of an integer
 *
 * Return - int
 */
int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
